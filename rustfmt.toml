# Token-efficient rustfmt: wider lines mean fewer wraps, which is fewer
# newline+indent tokens when a model reads the crate. Stable-only subset, since
# CI gates on stable `cargo fmt --all -- --check`.
max_width = 140
use_small_heuristics = "Max"
fn_params_layout = "Compressed"
